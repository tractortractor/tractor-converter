INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/lib/volInt)



SET(VOLINT_SOURCES
  volInt.cpp
  )

SET(VOLINT_HEADER_FILES
  volInt.hpp
  )

ADD_LIBRARY(volInt STATIC
  ${VOLINT_SOURCES}
  ${VOLINT_HEADER_FILES}
  )
